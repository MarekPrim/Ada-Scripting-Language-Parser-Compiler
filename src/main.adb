with Ada.Text_IO, Ada.Integer_Text_IO, intermediaire, operateur;
use Ada.Text_IO, Ada.Integer_Text_IO, intermediaire, operateur;

procedure main is
   fileName : constant string := "facto.src";
begin
   
   traiterProgramme(fileName);

end main;